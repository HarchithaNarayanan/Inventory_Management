<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <!--
        PARENT: Spring Boot 3.2.5 (stable, supports Java 17 + Jakarta EE 10)
        FIX: Was 4.1.0 which does not exist — caused Maven resolution failure
    -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.2.5</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>

    <groupId>com.inventory</groupId>
    <artifactId>inventory-management</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>inventory-management</name>
    <description>Inventory Management System — Spring Boot 3 + MySQL</description>

    <properties>
        <java.version>17</java.version>
    </properties>

    <dependencies>

        <!-- ======================================================
             CORE SPRING BOOT STARTERS
        ====================================================== -->

        <!-- Spring Data JPA — ORM layer with Hibernate for MySQL -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>

        <!-- Bean Validation (JSR-380) — @NotBlank, @NotNull, @Size etc. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!--
            Spring Web MVC — REST controller support, embedded Tomcat
            FIX: Was spring-boot-starter-webmvc (invalid) → spring-boot-starter-web
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- ======================================================
             DATABASE
        ====================================================== -->

        <!-- MySQL JDBC Driver (runtime only — not needed at compile time) -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ======================================================
             DEVELOPER PRODUCTIVITY
        ====================================================== -->

        <!--
            Lombok — auto-generates getters, setters, constructors,
            builders via annotations. Marked optional so it is NOT
            included in the final packaged JAR.
        -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ======================================================
             TEST
             FIX: Removed non-existent artifacts:
               - spring-boot-starter-data-jpa-test
               - spring-boot-starter-validation-test
               - spring-boot-starter-webmvc-test
             Replaced with the single correct test starter.
        ====================================================== -->

        <!-- Spring Boot Test — JUnit 5, Mockito, MockMvc, DataJpaTest -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- ======================================================
             SECURITY
        ====================================================== -->

        <!--
            Spring Security — provides authentication, authorization,
            and CSRF protection. Once on the classpath, all endpoints
            are locked until SecurityConfig explicitly opens them.
        -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <!--
            JJWT API — public interface for building and parsing
            JSON Web Tokens. compile-scope so our code can reference
            Jwts, Claims, JwtBuilder, etc.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.3</version>
        </dependency>

        <!--
            JJWT Implementation — runtime engine for JJWT.
            runtime scope: not needed at compile time, only when app runs.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

        <!--
            JJWT Jackson — allows JJWT to use Jackson for JSON
            serialization of JWT claims. runtime scope.
        -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.3</version>
            <scope>runtime</scope>
        </dependency>

    </dependencies>

    <build>
        <plugins>

            <!-- Spring Boot Maven Plugin — packages fat JAR, excludes Lombok -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>

            <!--
                Maven Compiler Plugin — ensures Lombok annotation processor
                runs during both compile and test-compile phases
            -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

        </plugins>
    </build>

</project>
